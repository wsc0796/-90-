# final_report

## 1. 覆盖情况

已完成对学习通 81 道独立题的整合。网络概述、物理层、数据链路层、网络层、运输层都已落到覆盖矩阵、客观题、简答题、大题模板、模拟卷和最终版里。

## 2. 已新增的老师口径

- 对等实体、SAP、服务原语。
- 教学四层与课堂五层的区分。
- `PPP` 不可靠。
- `网桥/二层交换机` 不隔离广播域。
- `ARP` 跨网查默认网关 MAC。
- `Ping` 使用 ICMP 回送请求/回答。
- `rwnd` 与 `cwnd` 的区别。
- `TCP` 确认号和序号语义。
- `TCP` 半关闭。
- 数据链路层滑动窗口与信道利用率。

## 3. 大题口径

已强化并统一：

- 时延计算。
- CRC。
- CSMA/CD 最短帧和最大距离。
- 子网掩码、CIDR、默认路由、最长前缀匹配。
- 路由聚合。
- RIP 更新。
- ARP 跨网流程和 ARP 次数。
- TCP 序号确认号。
- 慢开始、拥塞避免、超时后的 `ssthresh/cwnd`。
- 三次握手、四次挥手、半关闭。
- 数据链路层滑动窗口、GBN、SR、信道利用率、满利用率窗口反求。

## 4. 题型结构

已按 `10-10-10-30-40` 结构生成原题口径卷和变式查漏卷。

## 5. 是否存在冲突

已检查并避免以下错误：

- 把 `TCP/IP` 误写成只按五层作答。
- 把 `PPP` 误写成可靠协议。
- 把网桥/交换机误写成隔离广播域。
- 混淆 `rwnd` 和 `cwnd`。
- 混淆 ACK 号和最后已收序号。
- 漏掉分类地址题。
- 把链路层窗口利用率混成 TCP 窗口。

## 6. 结论

approved
