# 05_客观题速记册

> 覆盖：选择题(易混概念对比) + 填空题(必背数字与公式) + 判断题(50条陷阱)
> 用法：零碎时间读，考前半小时刷一遍

---

# 第一部分：选择题易混概念对比

## 对比1：TCP vs UDP

| 维度 | TCP | UDP |
|------|-----|-----|
| 连接 | 面向连接 | 无连接 |
| 可靠性 | 可靠，确认+重传 | 不可靠，尽最大努力 |
| 数据单位 | 面向字节流 | 面向报文(保留边界) |
| 流量控制 | 有(滑动窗口) | 无 |
| 拥塞控制 | 有(四阶段) | 无 |
| 首部开销 | 至少20B(可变) | 固定8B |
| 适用场景 | Web/文件/邮件 | DNS/视频/语音/实时 |
| 通信方式 | 一对一全双工 | 一对一/一对多/多对多 |

## 对比2：交换机 vs 路由器

| 维度 | 交换机(Switch) | 路由器(Router) |
|------|------|------|
| 工作层 | 数据链路层 | 网络层 |
| 转发依据 | MAC地址(48bit) | IP地址(32bit) |
| 转发单位 | 帧(Frame) | 数据报(Datagram) |
| 隔离冲突域 | ✅(每端口一个) | ✅ |
| 隔离广播域 | ❌(默认不隔离) | ✅ |
| 自学习 | ✅(学习源MAC) | ❌(需配置路由表) |
| 连接网络 | 同一网络内 | 不同网络间 |

## 对比3：流量控制 vs 拥塞控制

| 维度 | 流量控制 | 拥塞控制 |
|------|---------|---------|
| 控制对象 | 发送速率vs接收能力 | 发送速率vs网络能力 |
| 原因 | 接收方缓冲区满 | 网络拥塞/路由器缓存溢出 |
| 依赖变量 | rwnd(接收窗口) | cwnd(拥塞窗口) |
| 实现 | 滑动窗口 | 慢开始/拥塞避免/快重传/快恢复 |
| 范围 | 端到端(接收方→发送方) | 全网范围 |

## 对比4：奈奎斯特 vs 香农

| 维度 | 奈奎斯特(Nyquist) | 香农(Shannon) |
|------|------|------|
| 条件 | 理想**无噪声**信道 | **有噪声**信道 |
| 公式 | C = 2W × log₂(V) | C = W × log₂(1 + S/N) |
| 限制因素 | 带宽W + 电平数V | 带宽W + 信噪比S/N |
| 单位 | W:Hz, C:bps | W:Hz, S/N:比值(非dB) |
| 实际应用 | 无噪声理论上限 | 有噪声理论上限 |

## 对比5：GBN vs SR

| 维度 | GBN(后退N帧) | SR(选择重传) |
|------|------|------|
| 接收窗口 | 通常=1 | >1 |
| 乱序帧 | 丢弃 | 缓存 |
| 确认方式 | 累计确认 | 独立逐一确认 |
| 超时重传 | 从丢失帧起全重传 | 只重传丢失帧 |
| 计时器 | 最早未确认帧1个 | 每帧1个 |
| 发送窗口上限 | ≤ 2^n - 1 | ≤ 2^(n-1) |
| 3bit序号最大窗口 | 7 | 4 |

## 对比6：冲突域 vs 广播域

| 维度 | 冲突域 | 广播域 |
|------|--------|--------|
| 含义 | 同一时刻只能一台发 | 广播帧可达范围 |
| 集线器 | 1个冲突域 | 1个广播域 |
| 交换机 | 每端口1个冲突域 | 默认1个广播域(同VLAN) |
| 路由器 | 隔离冲突域 | 隔离广播域 |
| VLAN | 不隔离冲突域 | **隔离广播域** ←易错 |

## 对比7：发送时延 vs 传播时延

| 维度 | 发送时延(传输时延) | 传播时延 |
|------|------|------|
| 定义 | 将分组所有bit推上链路的时间 | 信号在链路上传播的时间 |
| 公式 | L(bit) / R(bps) | d(m) / v(m/s) |
| 影响因素 | 分组长度+链路速率 | 链路距离+传播速度 |
| 类比 | 车队全部上高速的时间 | 第一辆车在高速上跑的时间 |

## 对比8：电路交换 vs 分组交换

| 维度 | 电路交换 | 分组交换 |
|------|---------|---------|
| 资源分配 | 预先独占 | 按需共享(统计复用) |
| 建连 | 需要建立专用通路 | 数据报方式不需要 |
| 时延 | 相对固定 | 受排队影响，可变 |
| 效率 | 空闲也占资源 | 链路利用率高 |
| 典型 | 电话网 | Internet |

## 对比9：单播 vs 广播 vs 泛洪

| 概念 | 含义 | 触发条件 |
|------|------|---------|
| 单播 | 一对一发送 | 交换机已知目的MAC对应端口 |
| 广播 | 发往所有主机(目的MAC全F) | ARP请求、DHCP Discover |
| 泛洪 | 交换机向除入端口外的所有端口转发 | 目的MAC未知(未知单播) |

## 对比10：CSMA/CD vs CSMA/CA

| 维度 | CSMA/CD | CSMA/CA |
|------|---------|---------|
| 使用场景 | 有线共享式以太网 | 无线局域网(WiFi) |
| 核心策略 | 检测碰撞 | 避免碰撞 |
| 发送中 | 边发边听 | 预约/等待 |
| 链路层ACK | 不使用 | 使用 |
| 解决 | 隐蔽站问题不用管 | 隐蔽站需RTS/CTS |

---

# 第二部分：填空题必背数字与公式

## 地址与标识符

| 项目 | 数值 | 记忆口诀 |
|------|:---:|---------|
| MAC地址 | **48 bit** | "48卖克" |
| IPv4地址 | **32 bit** | "32IP" |
| IPv6地址 | **128 bit** | "6比4大，128比32大" |
| 端口号 | **16 bit** (0-65535) | "16端口" |
| TCP首部最小 | **20 B** | "二0" |
| UDP首部 | **8 B** | "U8" |
| IP首部最小 | **20 B** | 同TCP |

## 以太网帧

| 项目 | 数值 |
|------|:---:|
| 最小帧长(不含前导码/SFD) | **64 B** |
| 最大帧长(不含前导码) | **1518 B** |
| 数据字段 | **46 - 1500 B** |
| FCS | **4 B** |
| 前导码 | 7 B |
| SFD(帧开始定界符) | 1 B |
| 目的MAC | 6 B |
| 源MAC | 6 B |
| 类型/长度字段 | 2 B |

## RIP

| 项目 | 数值 |
|------|:---:|
| 最大跳数(不可达) | **16** |
| 更新计时器 | 约30 s |
| 失效计时器 | 约180 s |
| 删除计时器 | 约240 s |

## 常用端口号

| 协议 | 端口 | 运输层协议 |
|------|:---:|:---:|
| DNS | 53 | UDP(也可TCP) |
| DHCP服务器 | 67 | UDP |
| DHCP客户端 | 68 | UDP |
| HTTP | 80 | TCP |
| HTTPS | 443 | TCP |
| FTP控制 | 21 | TCP |
| FTP数据 | 20 | TCP |
| SMTP | 25 | TCP |
| POP3 | 110 | TCP |
| SSH | 22 | TCP |
| TFTP | 69 | UDP |

> 考试范围不考应用层，但端口号可能在运输层填空题出现。

## 物理层

| 项目 | 公式/数值 |
|------|---------|
| 奈氏(无噪声) | **C = 2W × log₂(V)** |
| 香农(有噪声) | **C = W × log₂(1 + S/N)** |
| S/N(dB)换算 | S/N = **10^(dB/10)** |
| 10dB | S/N = 10 |
| 20dB | S/N = 100 |
| 30dB | S/N = 1000 |
| 40dB | S/N = 10000 |
| 发送时延 | **L / R** |
| 传播时延 | **d / v** |
| 比特率vs波特率 | 比特率 = 波特率 × **log₂(M)** |
| 每码元比特数 | **log₂(M)** |

## 数据链路层

| 项目 | 公式/数值 |
|------|---------|
| CSMA/CD争用期 | **2τ** (往返传播时延) |
| 最小帧长 | **Lmin = 2d × R / v** |
| 最大距离 | **Dmax = L × v / (2R)** |
| CRC步骤 | 补r个0 → 模2除 → 余数 |

## 网络层

| 项目 | 公式/数值 |
|------|---------|
| 子网主机位 | h = **32 - 前缀长度** |
| 地址总数 | **2^h** |
| 可用主机数 | **2^h - 2** |
| IP分片偏移单位 | **8 字节** |
| MF=0 | 最后一片 |
| MF=1 | 后面还有 |

## 运输层

| 项目 | 公式/数值 |
|------|---------|
| 发送窗口 | **min(rwnd, cwnd)** |
| SYN消耗序号 | **1** 个 |
| FIN消耗序号 | **1** 个 |
| TIME-WAIT | **2 MSL** |
| 慢开始cwnd | **指数增长(翻倍)** |
| 拥塞避免cwnd | **线性增长(+1MSS/RTT)** |
| 快重传触发 | **3个重复ACK** |
| 快恢复ssthresh | **cwnd/2** |
| TCP面向 | **字节流** |
| UDP面向 | **报文** |
| GBN窗口上限 | **2^n - 1** |
| SR窗口上限 | **≤ 2^(n-1)** |

---

# 第三部分：判断题50条陷阱

> 格式：题目 → 答案 → 错误原因 → 改正

### 物理层 (1-12)

**1. 物理层PDU是帧。**
❌ 错误。物理层PDU是**比特(Bit)**，帧是数据链路层PDU。

**2. 半双工允许双方同时发送。**
❌ 错误。半双工是双向但**不能同时**。全双工才能同时收发。

**3. 基带信号一定是数字信号。**
❌ 错误。基带信号可以是数字也可以是模拟，关键是**未经过调制**。

**4. 集线器可以根据MAC地址表转发。**
❌ 错误。集线器工作在**物理层**，不认识MAC地址，直接向所有端口转发信号。

**5. 交换机隔离广播域。**
❌ 错误。普通二层交换机默认**不**隔离广播域，只能隔离**冲突域**。路由器才隔离广播域。

**6. 香农公式中的S/N可以直接代入dB值计算。**
❌ 错误。必须先将dB换算为**比值**(S/N=10^(dB/10))，再代入公式。

**7. 奈氏准则适用于有噪声信道。**
❌ 错误。奈氏准则适用于**理想无噪声**信道。有噪声信道用香农公式。

**8. 10Base-T中的"Base"表示宽带传输。**
❌ 错误。Base表示**基带**传输，不是宽带。

**9. ADSL通常上下行速率对称。**
❌ 错误。ADSL是**非对称**数字用户线(A=Asymmetric)，下行速率通常高于上行。

**10. 中继器和集线器都能隔离冲突域。**
❌ 错误。中继器和集线器都工作在物理层，**不能**隔离冲突域。

**11. 单模光纤比多模光纤更适合短距离通信。**
❌ 错误。单模光纤适合**远距离**，多模光纤适合短距离(成本低)。

**12. CDMA中码片内积为0表示该用户发送了比特0。**
❌ 错误。内积为0表示该用户**未发送数据**。+1表示发送1，-1表示发送0。

### 数据链路层 (13-30)

**13. CRC可以自动纠正传输错误。**
❌ 错误。CRC只能**检错**，不能**纠错**。发现错误后通常请求重传。

**14. 透明传输表示数据内容被加密。**
❌ 错误。透明传输是指数据中的特殊字符/比特**不会**被误判为控制信息(帧边界等)。不是加密。

**15. PPP协议提供可靠传输。**
❌ 错误。PPP**不提供**可靠传输(无序号、无确认、无重传)，只提供检错和不确认的无连接服务。

**16. 以太网最小帧长72字节。**
❌ 错误。最小帧长**64字节**(不含前导码和SFD)。含前导码和SFD才是72字节，考试通常以64字节为准。

**17. 前导码和SFD参与FCS计算。**
❌ 错误。前导码(7B)和SFD(1B)**不计入**帧长，也**不参与**FCS计算。

**18. CSMA/CD适用于现代全双工交换式以太网。**
❌ 错误。CSMA/CD适用于**半双工共享式**以太网。现代全双工交换式以太网不使用CSMA/CD。

**19. 交换机收到未知目的MAC地址的帧时直接丢弃。**
❌ 错误。未知单播帧会被**泛洪**(向除入端口外的所有端口转发)。

**20. 交换机收到目的端口等于入端口的帧时，向所有端口广播。**
❌ 错误。目的端口等于入端口时，交换机**过滤**(丢弃)该帧，因为目标已在同一端口。

**21. ARP广播可以穿越路由器。**
❌ 错误。ARP广播是链路层广播，**不能**穿越路由器。路由器隔离广播域。跨网通信时ARP查询默认网关MAC。

**22. 路由器隔离冲突域但不隔离广播域。**
❌ 错误。路由器**既**隔离冲突域**也**隔离广播域。

**23. VLAN主要用于划分冲突域。**
❌ 错误。VLAN主要用于划分**广播域**，不是冲突域。

**24. GBN协议接收方可以缓存乱序帧。**
❌ 错误。GBN接收窗口=1，收到乱序帧直接**丢弃**。只有SR才缓存乱序帧。

**25. 3bit序号时SR窗口最大为7。**
❌ 错误。SR窗口上限≤2^(n-1)=4，GBN才≤2^n-1=7。

**26. 数据链路层的三个基本问题是路由选择、流量控制和拥塞控制。**
❌ 错误。三个基本问题是**封装成帧、透明传输、差错检测**。路由选择、流量控制和拥塞控制是网络层/运输层的功能。

**27. 以太网帧的数据字段最少为0字节。**
❌ 错误。数据字段最少**46字节**（不足时需填充），最多1500字节。

**28. 交换机一个端口只能学习一个MAC地址。**
❌ 错误。一个交换机端口可以学习**多个**MAC地址(比如端口下接了一台集线器或另一台交换机)。

**29. 跨网段通信时，源主机ARP查询目标主机的MAC地址。**
❌ 错误。跨网段通信时，ARP查询**默认网关**的MAC地址，不是目标主机的MAC地址。

**30. CSMA/CD的争用期是指数据帧的发送时间。**
❌ 错误。争用期是指信号在总线上**往返传播**的时间(2τ)，不是发送时间。

### 网络层 (31-42)

**31. IP地址和MAC地址都可以在全球范围内唯一标识一台主机。**
❌ 错误。MAC地址在局域网内唯一标识一台主机，但不能用于跨网络寻址。IP地址负责全球寻址。

**32. ICMP报文封装在UDP中传输。**
❌ 错误。ICMP封装在**IP数据报**中，不是UDP中。

**33. IPv6中路由器可以对过大的数据报进行分片。**
❌ 错误。IPv6中路由器**不**对数据报分片。如果MTU过大，路由器丢弃并发送ICMPv6 Packet Too Big报文。

**34. RIP使用跳数作为度量，最大跳数为15表示不可达。**
❌ 错误。最大跳数**16**表示不可达，15是最大有效跳数。

**35. CIDR中前缀越短，表示的地址块越小。**
❌ 错误。前缀越短，主机位越多，地址块**越大**。/8比/24的地址块大得多。

**36. 子网掩码255.255.255.252的可用主机数是4。**
❌ 错误。/30的可用主机数是**2**(4-2，减去网络地址和广播地址)。

**37. NAT只能转换IP地址，不能转换端口号。**
❌ 错误。NAPT/PAT可以同时转换IP地址**和**端口号，实现多个内网主机共用一个公网IP。

**38. 路由聚合就是把前缀越分越细。**
❌ 错误。路由聚合是把多个小地址块**合并**为一个大地址块(前缀变短)，目的是缩小路由表。分得越细叫子网划分。

**39. 直接交付和间接交付的区别在于是否经过路由器。**
✅ 正确。直接交付：目的主机在同一网络(不经过路由器)；间接交付：目的主机在不同网络(经过路由器)。

**40. 路由器转发分组时不改变源和目的IP地址。**
✅ 正确。路由器转发IP数据报时**不改变**源和目的IP地址(不同于NAT)。

**41. IP分片中，除最后一片外，每片数据部分必须是8字节的倍数。**
✅ 正确。因为片偏移以8字节为单位。

**42. 路由器收到一个IP数据报后，解封装到网络层，查路由表转发，重新封装链路层帧。**
✅ 正确。这就是路由器的标准转发过程。

### 运输层 (43-50)

**43. UDP提供可靠的数据传输服务。**
❌ 错误。UDP是**不可靠**的，尽最大努力交付，不保证可靠性。

**44. TCP的发送窗口大小仅由拥塞窗口cwnd决定。**
❌ 错误。发送窗口 = **min(rwnd, cwnd)**，同时受流量控制(rwnd)和拥塞控制(cwnd)约束。

**45. TCP三次握手中，SYN报文不消耗序号。**
❌ 错误。SYN报文**消耗1个序号**。同样FIN也消耗1个序号。

**46. TCP四次挥手中，被动关闭方收到FIN后立即发送FIN。**
❌ 错误。被动关闭方收到FIN后先发送**ACK**，等自己的数据也发完了再发FIN。所以是四次挥手而不是三次。

**47. TIME-WAIT状态只出现在客户端。**
❌ 错误。TIME-WAIT出现在**主动关闭**方，不一定是客户端。谁先发FIN谁就进入TIME-WAIT。

**48. 慢开始阶段，拥塞窗口每经过一个RTT就增加1个MSS。**
❌ 错误。慢开始阶段cwnd**指数增长(翻倍)**，拥塞避免阶段才是每RTT增加1个MSS(或按公式线性增长)。

**49. 快重传在发送方收到2个重复ACK时触发。**
❌ 错误。快重传在收到**3个**重复ACK(共4个相同ACK)时触发。

**50. 流量控制和拥塞控制是同一个概念的不同说法。**
❌ 错误。流量控制针对**接收方能力**(rwnd)，是端到端的；拥塞控制针对**网络能力**(cwnd)，是全局性的。两者机制不同。
